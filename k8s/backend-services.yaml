apiVersion: v1
kind: Service
metadata:
  namespace: default
  name: campuslink-auth
spec:
  selector:
    app: campuslink-auth
  ports:
  - port: 5100
    targetPort: 5100
  type: ClusterIP

---
apiVersion: v1
kind: Service
metadata:
  namespace: default
  name: campuslink-messaging
spec:
  selector:
    app: campuslink-messaging
  ports:
  - port: 5101
    targetPort: 5101
  type: ClusterIP

---
apiVersion: v1
kind: Service
metadata:
  namespace: default
  name: campuslink-content
spec:
  selector:
    app: campuslink-content
  ports:
  - port: 5102
    targetPort: 5102
  type: ClusterIP

---
apiVersion: v1
kind: Service
metadata:
  namespace: default
  name: campuslink-users
spec:
  selector:
    app: campuslink-users
  ports:
  - port: 5103
    targetPort: 5103
  type: ClusterIP

---
apiVersion: v1
kind: Service
metadata:
  namespace: default
  name: campuslink-files
spec:
  selector:
    app: campuslink-files
  ports:
  - port: 5106
    targetPort: 5106
  type: ClusterIP
