{
  "name": "student-social-network-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-all": "npm install --prefix backend",
    "test-all": "npm test --prefix backend -- --coverage --watchAll=false"
  }
}
